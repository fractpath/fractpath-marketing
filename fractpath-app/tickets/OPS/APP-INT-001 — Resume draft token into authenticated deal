# APP-INT-001 — Resume draft token into authenticated deal

## Sprint
Sprint 0 (alignment-only) → Sprint 5 (implementation)

## Objective
Define the authoritative app-side behavior for redeeming a draft token
(created during the marketing flow) and resuming it into an authenticated,
permissioned deal context.

This ticket establishes the **only valid bridge** from:
marketing proto-state → real app deal state.

## Context (Locked Decisions)
- Marketing may never create real deals.
- Widget produces DraftSnapshot but does not persist or mint tokens.
- App is the sole authority for:
  - draft-token minting
  - draft-token redemption
  - deal creation
- Draft tokens are non-binding and pre-auth.
- No real deal exists until authentication is complete.

## Definitions
- **DraftSnapshot**: Widget-produced snapshot of inputs + computed outputs.
- **Draft Token**: App-minted token referencing a stored DraftSnapshot.
- **Resume Flow**: The process of redeeming a draft token and initializing a deal.
- **Authenticated Deal**: A persisted, permissioned deal owned by an app user.

## Draft Token Redemption Flow

### Entry Point
- User arrives via:
  - `/resume?token=...`
  - or equivalent deep link from marketing share email

### Redemption Rules
- Draft token must:
  - exist
  - be unexpired
  - not already redeemed (unless idempotent re-entry is explicitly allowed)
- Draft token redemption requires authentication:
  - unauthenticated users must authenticate first
  - token is preserved across auth redirect

### Redemption Steps (Conceptual)
1. Validate draft token
2. Load associated DraftSnapshot
3. Authenticate user (if not already)
4. Create a new deal record OR attach snapshot to an existing draft deal
5. Persist snapshot data according to contract version
6. Transition user into authenticated deal view

## Deal Creation Semantics
- DraftSnapshot fields are treated as:
  - user-provided inputs
  - widget-computed outputs
- Snapshot data is:
  - non-binding
  - editable post-resume
  - versioned per app deal lifecycle rules
- No counterparty commitments exist at resume time.

## Permissions & Ownership
- The authenticated user who redeems the token becomes:
  - the initial deal owner
- All subsequent access control is enforced via app auth rules.
- Marketing-originated shares grant **no implicit permissions**.

## Failure Modes (Explicit)
The app must handle:
- expired tokens
- invalid tokens
- already-redeemed tokens
- contract-version mismatch (graceful failure or upgrade path)

No silent fallback or partial deal creation is allowed.

## Prohibitions
The app must NOT:
- Trust client-side DraftSnapshot data directly
- Allow marketing or widget code to create deals
- Infer missing fields not present in the DraftSnapshot contract
- Treat draft data as a binding offer

## Acceptance Criteria
- Draft tokens cleanly bridge marketing → app.
- No deal exists before authentication.
- DraftSnapshot is fully auditable and versioned.
- Resume flow is deterministic and repeatable.
- All behavior aligns with OPS-INT-001 orchestration rules.

## Dependencies
- OPS-INT-001 — Multi-repo orchestration + contract enforcement
- WGT-INT-001 — Widget public interface
- WGT-040 — Draft snapshot schema + stability guarantees
- MKT-006 — Lead + share API routes
