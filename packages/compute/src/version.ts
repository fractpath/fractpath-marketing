/**
 * Compute versioning contract
 *
 * - COMPUTE_SEMVER: stable semantic version for compatibility checks.
 * - COMPUTE_VERSION: build identifier (no -dev once contract is locked).
 */

export const COMPUTE_SEMVER = "10.1.0" as const;
export const COMPUTE_VERSION = "10.1.0" as const;
