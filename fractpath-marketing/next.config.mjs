import { fileURLToPath } from "url"

/** @type {import('next').NextConfig} */
const nextConfig = {
  // NOTE: migrated from next.config.ts because this Next.js build does not support next.config.ts here.
}

export default nextConfig
