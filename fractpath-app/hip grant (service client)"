[1mdiff --git a/src/app/api/deals/[dealId]/fork/route.ts b/src/app/api/deals/[dealId]/fork/route.ts[m
[1mindex 9fe5415..408334b 100644[m
[1m--- a/src/app/api/deals/[dealId]/fork/route.ts[m
[1m+++ b/src/app/api/deals/[dealId]/fork/route.ts[m
[36m@@ -1,5 +1,6 @@[m
 import { NextRequest, NextResponse } from "next/server";[m
 import { createClient } from "@/lib/supabase/server";[m
[32m+[m[32mimport { createServiceClient } from "@/lib/supabase/service";[m
 import { getLatestDealSnapshot } from "@/lib/dealSnapshotDb";[m
 [m
 function jsonError(message: string, status = 400) {[m
[36m@@ -18,52 +19,57 @@[m [mexport async function POST([m
 ) {[m
   const { dealId } = await context.params;[m
 [m
[31m-  if (!isUuid(dealId)) {[m
[31m-    return jsonError("Invalid deal ID", 400);[m
[31m-  }[m
[32m+[m[32m  if (!isUuid(dealId)) return jsonError("Invalid deal ID", 400);[m
 [m
[32m+[m[32m  // Auth is user-scoped[m
   const supabase = await createClient();[m
   const {[m
     data: { user },[m
   } = await supabase.auth.getUser();[m
 [m
[31m-  if (!user) {[m
[31m-    return jsonError("Unauthorized", 401);[m
[32m+[m[32m  if (!user) return jsonError("Unauthorized", 401);[m
[32m+[m
[32m+[m[32m  // All DB operations for fork use service client (RLS-safe for create)[m
[32m+[m[32m  const service = createServiceClient();[m
[32m+[m
[32m+[m[32m  const { data: originalDeal, error: dealError } = await ([m
[32m+[m[32m    service.from("deals") as any[m
[32m+[m[32m  )[m
[32m+[m[32m    .select("id, owner_user_id, mode")[m
[32m+[m[32m    .eq("id", dealId)[m
[32m+[m[32m    .maybeSingle();[m
[32m+[m
[32m+[m[32m  if (dealError || !originalDeal) {[m
[32m+[m[32m    return jsonError("Deal not found", 404);[m
   }[m
 [m
[31m-  // Enforce access via deal_access_grants (RLS-backed). Only OWNER/VIEWER exist.[m
[31m-  const { data: grant, error: grantError } = await (supabase[m
[31m-    .from("deal_access_grants") as any)[m
[32m+[m[32m  // Access check: user must at least be able to view the source deal[m
[32m+[m[32m  const { data: grant } = await (service.from("deal_access_grants") as any)[m
     .select("role")[m
     .eq("deal_id", dealId)[m
     .eq("user_id", user.id)[m
     .maybeSingle();[m
 [m
[31m-  if (grantError) {[m
[31m-    return jsonError("Failed to verify access", 500);[m
[31m-  }[m
[32m+[m[32m  const hasAccess =[m
[32m+[m[32m    originalDeal.owner_user_id === user.id ||[m
[32m+[m[32m    (grant?.role && ["OWNER", "VIEWER", "COUNTERPARTY"].includes(grant.role));[m
 [m
[31m-  if (!grant?.role || !["OWNER", "VIEWER"].includes(grant.role)) {[m
[32m+[m[32m  if (!hasAccess) {[m
     return jsonError("Forbidden (no access to source deal)", 403);[m
   }[m
 [m
[31m-  // Viewer counter model: viewers fork; owners revise in-place via compute/save.[m
[31m-  if (grant.role === "OWNER") {[m
[31m-    return jsonError("OWNER cannot fork their own deal; use compute instead", 400);[m
[32m+[m[32m  // Owner self-fork is explicitly blocked[m
[32m+[m[32m  if (originalDeal.owner_user_id === user.id || grant?.role === "OWNER") {[m
[32m+[m[32m    return jsonError([m
[32m+[m[32m      "OWNER cannot fork their own deal; use compute instead",[m
[32m+[m[32m      400,[m
[32m+[m[32m    );[m
   }[m
 [m
[31m-  // Load source deal (read should be permitted by RLS if viewer has access).[m
[31m-  const { data: originalDeal, error: dealError } = await (supabase.from("deals") as any)[m
[31m-    .select("id, mode")[m
[31m-    .eq("id", dealId)[m
[31m-    .maybeSingle();[m
[31m-[m
[31m-  if (dealError || !originalDeal) {[m
[31m-    return jsonError("Deal not found", 404);[m
[31m-  }[m
[31m-[m
[31m-  // Create new deal owned by the requester.[m
[31m-  const { data: newDeal, error: insertDealError } = await (supabase.from("deals") as any)[m
[32m+[m[32m  // Create new deal owned by requester[m
[32m+[m[32m  const { data: newDeal, error: insertDealError } = await ([m
[32m+[m[32m    service.from("deals") as any[m
[32m+[m[32m  )[m
     .insert({[m
       owner_user_id: user.id,[m
       status: "IMPORTED",[m
[36m@@ -79,30 +85,37 @@[m [mexport async function POST([m
     return jsonError("Failed to create forked deal", 500);[m
   }[m
 [m
[31m-  // Grant OWNER on new deal to the requester.[m
[31m-  const { error: newGrantError } = await (supabase.from("deal_access_grants") as any).insert([m
[32m+[m[32m  // Ensure explicit OWNER grant on the new deal (idempotent).[m
[32m+[m[32m  // If a trigger already created this row, ignore duplicates.[m
[32m+[m[32m  const { error: grantError } = await ([m
[32m+[m[32m    service.from("deal_access_grants") as any[m
[32m+[m[32m  ).upsert([m
     {[m
       deal_id: newDeal.id,[m
       user_id: user.id,[m
       role: "OWNER",[m
       created_by: user.id,[m
     },[m
[32m+[m[32m    {[m
[32m+[m[32m      onConflict: "deal_id,user_id",[m
[32m+[m[32m      ignoreDuplicates: true,[m
[32m+[m[32m    },[m
   );[m
 [m
[31m-  if (newGrantError) {[m
[31m-    console.error("grant insert error:", newGrantError.message);[m
[32m+[m[32m  if (grantError) {[m
[32m+[m[32m    console.error("grant upsert error:", grantError);[m
     return jsonError("Failed to assign ownership on forked deal", 500);[m
   }[m
 [m
[31m-  // Best-effort copy of latest snapshot from source deal into new deal.[m
[32m+[m[32m  // Copy latest snapshot as a baseline if one exists[m
   let baselineSnapshotId: string | null = null;[m
[32m+[m[32m  const latestResult = await getLatestDealSnapshot(service, dealId);[m
 [m
[31m-  const latestResult = await getLatestDealSnapshot(supabase as any, dealId);[m
   if (latestResult.ok && latestResult.snapshot) {[m
     const src = latestResult.snapshot;[m
 [m
     const { data: copiedSnap, error: snapError } = await ([m
[31m-      supabase.from("deal_snapshots") as any[m
[32m+[m[32m      service.from("deal_snapshots") as any[m
     )[m
       .insert({[m
         deal_id: newDeal.id,[m
[36m@@ -123,8 +136,10 @@[m [mexport async function POST([m
     }[m
   }[m
 [m
[31m-  // Audit event (best-effort)[m
[31m-  const { error: eventError } = await (supabase.from("deal_events") as any).insert({[m
[32m+[m[32m  // Audit event[m
[32m+[m[32m  const { error: eventError } = await ([m
[32m+[m[32m    service.from("deal_events") as any[m
[32m+[m[32m  ).insert({[m
     deal_id: newDeal.id,[m
     event_type: "DEAL_CREATED",[m
     payload: {[m
